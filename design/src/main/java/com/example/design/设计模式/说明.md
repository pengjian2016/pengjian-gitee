---
<https://www.runoob.com/design-pattern/prototype-pattern.html>

#设计模式的六大原则
1. 开闭原则（对扩展开放，对修改关闭）
2. 里氏代换原则 （任何基类可以出现的地方，子类一定可以出现）
3. 依赖倒转原则（依赖抽象不依赖具体）
4. 接口隔离原则（强调降低依赖，降低耦合）
5. 迪米特法则-最少知道原则（功能模块相对独立）
6. 合成复用原则（尽量使用合成/聚合的方式，而不是使用继承）


## 创建型模式
***
* ### 工厂模式
>- 意图：定义一个创建对象的接口，让其子类自己决定实例化哪一个工厂类，工厂模式使其创建过程延迟到子类进行。   
>- 主要解决：主要解决接口选择的问题。
>- 何时使用：我们明确地计划不同条件下创建不同实例时。
>- 如何解决：让其子类实现工厂接口，返回的也是一个抽象的产品。
>- 关键代码：创建过程在其子类执行。
>- 应用实例： 1、您需要一辆汽车，可以直接从工厂里面提货，而不用去管这辆汽车是怎么做出来的，以及这个汽车里面的具体实现。 2、Hibernate 换数据库只需换方言和驱动就可以。

* ### 抽象工厂模式
>- 意图：提供一个创建一系列相关或相互依赖对象的接口，而无需指定它们具体的类。
>- 主要解决：主要解决接口选择的问题。
>- 何时使用：系统的产品有多于一个的产品族，而系统只消费其中某一族的产品。
>- 如何解决：在一个产品族里面，定义多个产品。
>- 关键代码：在一个工厂里聚合多个同类产品。 
>- 应用实例

* ### 单例模式
>- 意图：保证一个类只有一个实例，并提供一个访问的全局访问点
>- 主要解决：一个全局使用的类频繁的创建和销毁
>- 何时使用：控制实例数目，节省系统资源
>- 如何解决：判断系统是否有这个实例，有则返回，无则创建
>- 关键代码：构造函数是私有的
>- 应用实例：一个班只有一个班主任 2生产唯一序列号。

* ### 建造者模式
>- 意图：将一个复杂的构建与其表示相分离，使得同样的构建过程可以创建不同的表示。
>- 主要解决：
>- 何时使用：一些基本部件不会变，而其组合经常变化的时候。
>- 如何解决：将变与不变分离开。
>- 关键代码：建造者：创建和提供实例 导演：管理建造出来的实例的依赖关系
>- 应用实例 1、去肯德基，汉堡、可乐、薯条、炸鸡翅等是不变的，而其组合是经常变化的，生成出所谓的"套餐"。
   2、JAVA 中的 StringBuilder

* ### 原型模式
>- 意图：创建对象的种类，并通过拷贝这些原型创建型的对象
>- 主要解决：在运行器建立和删除原型
>- 何时使用：
>- 如何解决：利用已有的一个原型对象，快速生成和原型一样的实例
>- 关键代码：克隆java继承Cloneable 重写clone
>- 应用实例：1细胞分裂 object的clone方法

***
## 结构型模式

* ### 适配器模式
>- 意图：将一个类的接口转换客户希望的另外一个接口
>- 主要解决：
>- 何时使用：
>- 如何解决：继承或者依赖（推荐）
>- 关键代码：适配器继承盒子依赖已有的对象，实现想要的目标接口
>- 应用实例：1美国电器 110V，中国 220V，就要有一个适配器将 110V 转化为 220V。2java的jdbc
* ### 装饰者模式
>- 意图：动态的一个对象添加一个额外的职责，让子类更灵活
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 代理模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 外观模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 桥接模式
>- 意图：将抽象部分和实现部分分离，使他们可以都可以独立的变化
>- 主要解决：在有多种可能会变化的情况，用继承会造成一些问题，扩展起来不灵活
>- 何时使用：多角度分离出来
>- 如何解决：抽象类的依赖实现
>- 关键代码：
>- 应用实例：墙上的开关，可以看到的开关是抽象的，不用管里面具体怎么实现的。
* ### 组合模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 享元模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
***

## 行为型模式
  
* ### 策略模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 模板方法模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 观察者模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 迭代器模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 责任链模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 命令模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 备忘录模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 状态模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 访问者模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 解释器模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 中介者模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：





