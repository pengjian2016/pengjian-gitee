---
<https://www.runoob.com/design-pattern/prototype-pattern.html>

#设计模式的六大原则
1. 开闭原则（对扩展开放，对修改关闭）
2. 里氏代换原则 （任何基类可以出现的地方，子类一定可以出现）
3. 依赖倒转原则（依赖抽象不依赖具体）
4. 接口隔离原则（强调降低依赖，降低耦合）
5. 迪米特法则-最少知道原则（功能模块相对独立）
6. 合成复用原则（尽量使用合成/聚合的方式，而不是使用继承）


## 创建型模式
***
* ### 工厂模式
>- 意图：定义一个创建对象的接口，让其子类自己决定实例化哪一个工厂类，工厂模式使其创建过程延迟到子类进行。   
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例： Hibernate 换数据库只需换方言和驱动就可以。
>            2您需要一辆汽车，可以直接从工厂里面提货，而不用去管这辆汽车是怎么做出来的，以及这个汽车里面的具体实现。

* ### 抽象工厂模式
>- 意图：提供一个创建一系列相关或相互依赖对象的接口，而无需指定它们具体的类。
>- 主要解决：
>- 何时使用：
>- 如何解决：在一个产品族里面，定义多个产品。
>- 关键代码： 
>- 应用实例  1、QQ 换皮肤，一整套一起换

* ### 单例模式
>- 意图：保证一个类只有一个实例，并提供一个访问的全局访问点
>- 主要解决：一个全局使用的类频繁的创建和销毁
>- 何时使用：
>- 如何解决：判断系统是否有这个实例，有则返回，无则创建
>- 关键代码：构造函数是私有的
>- 应用实例：一个班只有一个班主任 

* ### 建造者模式
- 意图：将一个复杂的构建与其表示相分离，使得同样的构建过程可以创建不同的表示。
- 主要解决：
- 何时使用：一些基本部件不会变，而其组合经常变化的时候。
- 如何解决：
- 关键代码：
- 应用实例 1、去肯德基，汉堡、可乐、薯条、炸鸡翅等是不变的，而其组合是经常变化的，生成出所谓的"套餐"。
           2、JAVA 中的 StringBuilder

* ### 原型模式
>- 意图：创建对象的种类，并通过拷贝这些原型创建型的对象
>- 主要解决：在运行器建立和删除原型
>- 何时使用：
>- 如何解决：利用已有的一个原型对象，快速生成和原型一样的实例
>- 关键代码：克隆java继承Cloneable 重写clone
>- 应用实例：1细胞分裂  2 object的clone方法

***
## 结构型模式

* ### 适配器模式
>- 意图：将一个类的接口转换客户希望的另外一个接口
>- 主要解决：
>- 何时使用：
>- 如何解决：继承类+实现接口
>- 关键代码：适配器继承盒子依赖已有的对象，实现想要的目标接口
>- 应用实例：1美国电器 110V，中国 220V，就要有一个适配器将 110V 转化为 220V
           2 在 LINUX 上运行 WINDOWS 程序
> 
* ### 装饰者模式
>- 意图：动态地给一个对象添加一些额外的职责。就增加功能来说，装饰器模式相比生成子类更为灵活
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码:
>- 应用实例：孙悟空有 72 变，当他变成"庙宇"后，他的根本还是一只猴子
> 
* ### 代理模式
>- 意图：为其他对象提供一种代理以控制对这个对象的访问。
>- 主要解决：
>- 何时使用：想在访问一个类时做一些控制。
>- 如何解决：。
>- 关键代码：
>- 应用实例： 1、Windows 里面的快捷方式
> 
* ### 外观模式
>- 意图：
>- 主要解决：降低访问复杂系统的内部子系统时的复杂度，简化客户端之间的接口。
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：1、电脑开关机 cup 内存通电情况   2、JAVA 的三层开发模式。
> 

* ### 桥接模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：给正方形，长方形上色 ，颜色有黑白
> 
* ### 组合模式
>- 意图： 将对象组合成树形结构以表示"部分-整体"的层次结构。组合模式使得用户对单个对象和组合对象的使用具有一致性。
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：如树形菜单
> 

* ### 享元模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：1、JAVA 中的 String，如果有则返回，如果没有则创建一个字符串保存在字符串缓存池里面。 2、数据库的数据池。
***

## 行为型模式
  
* ### 策略模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 模板方法模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 观察者模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 迭代器模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 责任链模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 命令模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 备忘录模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 状态模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 访问者模式
>- 意图：
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例：
* ### 解释器模式
>- 意图：对于一些固定文法构建一个解释句子的解释器。
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例： 加减法
* ### 中介者模式
>- 意图： 2、想通过一个中间类来封装多个类中的行为，而又不想生成太多的子类。
>- 主要解决：
>- 何时使用：
>- 如何解决：
>- 关键代码：
>- 应用实例： 3、MVC 框架，其中C（控制器）就是 M（模型）和 V（视图）的中介者。





